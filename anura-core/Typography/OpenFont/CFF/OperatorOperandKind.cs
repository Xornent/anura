namespace Anura.Typography.OpenFont.CFF {
    internal enum OperatorOperandKind {
        SID,
        Boolean,
        Number,
        Array,
        Delta,
        NumberNumber,
        SID_SID_Number
    }
}